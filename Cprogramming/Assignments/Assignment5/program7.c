/*#include <stdio.h>

int main() {
    int rows, i, j;

    // Input the number of rows
    printf("Enter the number of rows: ");
    scanf("%d", &rows);

    // Loop to iterate through each row
    for (i = 0; i < rows; i++) {
        // Loop to print elements in each row
        for (j = 0; j < rows; j++) {
            // Calculate the element based on the pattern
            int element = i * i + 1 + j * 2;
            printf("%-6d", element);
        }
        printf("\n");
    }

    return 0;
}*/

