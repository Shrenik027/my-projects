/*#include <stdio.h>

int main() {
    int rows, i, j, value = 2;

    // Input the number of rows
    printf("Enter the number of rows: ");
    scanf("%d", &rows);

    // Loop to iterate through each row
    for (i = 0; i < rows; i++) {
        // Loop to print elements in each row
        for (j = 0; j < rows; j++) {
            printf("%-4d", value);
            value += 2 * (i + 1) + j * 3; // Adding consecutive odd numbers
        }
        printf("\n");
    }

    return 0;
}
*/
